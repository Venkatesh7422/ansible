- name: installing packages
  hosts: localhost
  tasks:
  - name: install packages in order
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - git
    - mysql
    - nginx
    - aaa